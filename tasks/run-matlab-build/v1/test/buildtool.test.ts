// Copyright 2024 The MathWorks, Inc.

import * as assert from "assert";

export default function suite() {
    describe("buildtool.ts test suite", () => {
        describe("run build", () => {
            it("ideally works", async () => {
                assert(true);
            });
        });
    });
}
